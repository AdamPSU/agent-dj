---
name: dj
description: Control Claude DJ (local Spotify coding companion). Use for play, status, sync, device, quit.
disable-model-invocation: true
---

Run the installed CLI `dj`.

- No args → `dj status`
- Else → `dj $ARGUMENTS`

Return the command output concisely. Do not add extra explanation unless the command fails.
