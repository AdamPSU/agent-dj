[project]
name = "agent-dj"
version = "0.2.0"
description = "Spotify now-playing statusline for Claude Code and OpenCode."

readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
  "beaupy>=3.10.0",
]

[project.scripts]
dj = "backend.cli:main"

[dependency-groups]
dev = [
  "pytest>=8.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/backend"]

[tool.hatch.build.targets.wheel.sources]
"src" = ""

[tool.pytest.ini_options]
testpaths = ["src/tests"]
pythonpath = ["src"]
